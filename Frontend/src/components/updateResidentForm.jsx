import { useState } from "react"

const UpdateResidentForm = ({ residentId, onSubmit, onCancel }) => {
 

    const handleSubmit = async (e) => {
        e.preventDefault()
    
    }

    return (
        <form onSubmit={handleSubmit} style={{ marginTop: '1rem' }}>
         
        </form>
    )
}

export default UpdateResidentForm

import { useState } from "react"

const UpdateApartmentUnit = ({ residentId, onSubmit, onCancel }) => {

    const handleSubmit = async (e) => {
        e.preventDefault()
       
    }

    return (
        <form onSubmit={handleSubmit} style={{ marginTop: '1rem' }}>
    
        </form>
    )
}

export default UpdateApartmentUnit
